// import { ICreateUser, UserEmptyState } from '@/models';
import { createSlice} from '@reduxjs/toolkit';


export const todoSlice = createSlice({
  name: 'todo',
  initialState: {} ,
  reducers: {
    // createUser: (state, { payload } ) => {
    //   return payload;
    // }
  }
});

// export const { createUser } = todoSlice.actions;    